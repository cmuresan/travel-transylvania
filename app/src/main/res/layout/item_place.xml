<layout>

    <android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:focusable="true"
        app:squareLayoutOrientation="vertical">

        <com.cristianmmuresan.traveltransylvania.customview.SquareImageView
            android:id="@+id/place_image"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:contentDescription="@string/place_image_content_description"
            android:scaleType="centerCrop"
            tools:src="@drawable/alba_iulia_citadel"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/place_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:gravity="center"
            android:fontFamily="sans-serif"
            android:paddingBottom="@dimen/margin_xx_small"
            android:paddingTop="@dimen/margin_xx_small"
            android:textSize="@dimen/text_size_large"
            android:textStyle="bold"
            android:textColor="@color/colorPrimaryText"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/place_image"
            tools:text="@string/name_placeholder" />
    </android.support.constraint.ConstraintLayout>
</layout>